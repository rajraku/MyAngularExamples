import { CompareEqualsDirective } from './compare-equals.directive';

describe('CompareEqualsDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareEqualsDirective();
    expect(directive).toBeTruthy();
  });
});
